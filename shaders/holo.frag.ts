// V0 fragment shader — placeholder. Real iridescence shader lands in week 1 day 4.
// See plans/v0-implementation-plan.md §4.3.

export default /* glsl */ `
precision mediump float;

void main() {
  gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
}
`;
