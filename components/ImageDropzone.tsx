"use client";

// V0 stub — file input + drag-drop + client-side resize/encode preview.
export interface ImageDropzoneProps {
  label: string;
  onChange?: (file: File | null) => void;
}

export default function ImageDropzone(_props: ImageDropzoneProps) {
  return null;
}
