// V0 stub — nanoid(21) wrapper. Implemented when `createCard` is built (week 2).
// Will pin nanoid as a dep at that time.
export function makeShareToken(): string {
  throw new Error("makeShareToken not yet implemented (week 2).");
}
