// V0 stub — server-side Supabase client (service role). Implemented in week 2.
// Must NEVER be imported by a client component or route.

export function createServerClient() {
  throw new Error("createServerClient not yet implemented (week 2).");
}
