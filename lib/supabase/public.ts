// V0 stub — public-URL helpers for storage objects. Implemented in week 2.
// V0 has no Supabase JS SDK on the client; URLs are deterministic from token.

export function publicCardImageUrl(_token: string, _side: "front" | "back"): string {
  throw new Error("publicCardImageUrl not yet implemented (week 2).");
}
